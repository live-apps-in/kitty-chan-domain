export enum PortalMsg {
  //Warnings
  MENTION_WARN = 'You cannot use mentions in Portal ⚠. This message will not be delivered to other Portals but can be seen by members within this server.',
  ATTACHMENT_WARN = `We don't allow attachments currently ⚠. We'll add support for media soon ;)`,
  URL_WARN = `We don't allow this URL ⚠. Avoid sending website links in this Portal.`,
}
